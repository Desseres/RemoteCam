# Copy to .env.deploy and enter this site's actual hosting settings.
# Never commit .env.deploy. Do not use the Image Viewer / TraceLens directory.
FTP_HOST=ftp.example.com
FTP_PORT=21
FTP_USER=your-remotecam-user
FTP_PASSWORD=your-password
FTP_REMOTE_PATH=/public_html/remotecam
